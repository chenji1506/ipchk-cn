# =====================================================================
# ipchk.cn 环境变量配置模板
#
# 复制本文件为 .env 后按需修改（.env 不入库，见 .gitignore）
# 用法：cp .env.example .env && docker compose up -d
# =====================================================================

# 是否下载本地 IP 数据库（ip2region / qqwry / GeoIP 等，约 200MB）
#   false = 跳过下载，IP 查询走在线 API（适合内网 / 快速启动）
#   true  = 下载本地库，归属地等接口有离线兜底（适合海外 VPS）
IPDB=true

# 后端 API 地址基址（前端页面调用后端接口用）
#   ⚠️ 注意保留尾部斜杠 /，否则前端拼接 URL 会出错（如 ipchk.cnv1/...）
#   生产部署：https://域名/（如 https://ipchk.cn/）
#   内网部署：http://服务器内网IP:8080/（如 http://192.168.1.72:8080/）
API_BASE=https://ipchk.cn/

# 以下为可选配置（默认值已在 docker-compose.yml 中设定，通常无需覆盖）

# 站点主域名：SEO canonical / og:image / sitemap 用（默认 https://ipchk.cn/）
#SITE_URL=https://ipchk.cn/

# 强制 IPv4 / IPv6 出口查询接口（首页探测用户出口地址用）
#   （单栈服务器可指向主域，双栈用 4./6. 子域名强制走对应协议）
#V4_API=https://4.ipchk.cn/ip
#V6_API=https://6.ipchk.cn/ip
#DUALSTACK_API=https://ipchk.cn/ip
